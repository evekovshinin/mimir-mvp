# mimir-mvp

Mimir — это система управления когнитивным контекстом для задач с высокой сложностью. Она моделирует эволюцию контекста как DAG (Directed Acyclic Graph), аналогично Git.

Название вдохновлено Мимиром из скандинавской мифологии — хранителем мудрости и знания.

**Отличительные черты:**
- 📊 **DAG-подобная архитектура**: Commits, branches, merges как в Git
- 📝 **Immutable снимки контекста**: Каждый commit содержит полный снимок контекста
- 🧩 **Поддержка merge**: Автоматическое объединение контекстов с нескольких веток
- 🔍 **Метрики когнитивной нагрузки**: Отслеживание сложности (cognitive_load, uncertainty)
- 🗂️ **PostgreSQL 14+**: Надежное хранилище данных
- ⚡ **CLI интерфейс**: Удобная командная строка на основе Typer

## 🎯 Концепция

Контекст — это не заметка.
Это версия мышления в конкретный момент времени.

Каждый commit:
* immutable
* содержит полный snapshot контекста
* может иметь одного или нескольких родителей
* образует DAG внутри задачи

## 🏗 Архитектура

* Python 3.11+
* PostgreSQL
* SQLAlchemy 2.0
* Typer (CLI)
* Alembic (миграции)

## Модель данных:

* Task
* ContextCommit
* CommitParent
* Branch

## 🚀 Установка
1. Клонировать репозиторий

`` git clone https://github.com/yourname/mimir-mvp.git
cd mimir-mvp ``

2. Создать виртуальное окружение

`` python -m venv venv
source venv/bin/activate ``

3. Установить зависимости

`` pip install -e .``

4. Настроить PostgreSQL

Создать БД:

`` CREATE DATABASE mimir; ``


Создать .env:

`` DATABASE_URL=postgresql+psycopg://user:password@localhost/mimir ``

5. Применить миграции

`` alembic upgrade head ``

## 🖥 Использование
### Создание задачи

`` mimir create-task "TASK-42"``

Создаётся задача и ветка main.

### Коммит контекста
`` mimir commit \
  --task TASK-42 \
  --branch main \
  --message "Validated DB hypothesis" \
  --context-file context.txt \
  --cognitive-load 3 \
  --uncertainty 4 ``

### Создание ветки
`` mimir branch create async-refactor --task TASK-42 --from main ``

### Просмотр истории
`` mimir history --task TASK-42 --branch main ``

## 🔎 Зачем это нужно?

Mimir позволяет:
* сокращать время возврата в задачу
* сохранять reasoning
* видеть альтернативные ветки решений
* анализировать глубину размышлений
* фиксировать неопределённость

## 🧪 Статус

Это MVP-версия.
Цель — проверить гипотезу:
Можно ли управлять контекстом как версионируемой системой?

## 🛣 Дальнейшие планы

Аналитика веток

Метрики когнитивной нагрузки

Интеграция с LLM для анализа reasoning

Семантический поиск

Web-интерфейс

## 📜 Лицензия

MIT